- ampserver : configparser config ini file for serial port setup
- ampserver : argument for set tty and for tty (writes to config file and reads from stdin at startup)
- ampserver : db optional argument for volume change
- ampclient : argument handling to be done by argparse
